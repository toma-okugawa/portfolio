# build output
dist/
.cache

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# Astro
.astro/

# ---- project-specific ----
# researchmap raw export: contains personal data (birth date, etc.).
# The site content is derived from it; keep the raw file out of the public repo.
researchmap.json

# reference material (Atsuki Seo's site source, for design study only — not ours to publish)
sample/

# local source assets not meant for deployment (original photos, etc.)
assets-src/
